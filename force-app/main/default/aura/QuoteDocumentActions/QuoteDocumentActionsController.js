({
	generateDocument : function( cmp, evt, helper ) {

		helper.generateDocument( cmp, evt, helper );

	},

	previewDocument : function( cmp, evt, helper ) {

		helper.previewDocument( cmp, evt, helper );

	}
})